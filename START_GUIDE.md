# 中国汽车市场智能分析平台 - 使用说明

## 🚀 快速启动（推荐）

```bash
# 1. 解压文件
tar -xzvf ami-final.tar.gz

# 2. 进入目录
cd auto-market-intelligence

# 3. 运行启动脚本
./start.sh
```

## 📋 系统要求

- **Node.js**: v18 或更高版本
- **npm**: v9 或更高版本

如果没有 Node.js，请从 https://nodejs.org 下载安装 LTS 版本。

## 🌐 访问网站

启动成功后，打开浏览器访问：
```
http://localhost:3000
```

## 🛠️ 手动启动（如果脚本失败）

```bash
cd auto-market-intelligence/apps/web
npm install
npm run dev
```

## 🐛 常见问题

### 1. 端口被占用
```bash
# 使用其他端口
npm run dev -- --port 3001
```

### 2. 依赖安装失败
```bash
# 清除缓存重新安装
cd apps/web
rm -rf node_modules package-lock.json
npm install
```

### 3. 显示不正常
确保使用 **开发模式** (`npm run dev`)，不要直接使用静态文件。

## 📞 帮助

如果仍有问题，请检查：
1. Node.js 版本: `node -v` (需要 v18+)
2. 网络连接是否正常（安装依赖需要网络）
3. 端口 3000 是否被占用
